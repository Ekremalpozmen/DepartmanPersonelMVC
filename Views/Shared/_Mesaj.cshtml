@model PersonelMVCUI.ViewModel.MesajViewModel

@if (Model.Status)
{
    <div class="alert alert-success">
        @Model.Mesaj <a href="@Model.Url">@Model.LinkText</a>
    </div>
}
else
{
    <div class="alert alert-danger">
        @Model.Mesaj <a href="@Model.Url">@Model.LinkText</a>
    </div>
}